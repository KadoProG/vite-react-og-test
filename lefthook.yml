pre-commit:
  parallel: true
  jobs:
    - run: pnpm biome:check:staged
      stage_fixed: true
pre-push:
  parallel: true
  jobs:
    - name: check-types
      run: pnpm tsc -b
      root: apps/for-vercel
      glob: apps/for-vercel/**
